#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
    printf("Size of char is: %zu", sizeof(char));
    printf("Size of integer is: %zu", sizeof(int));
    return (0);
}
